// Point the frontend at your Render backend. No trailing slash.
// e.g. window.API_BASE = "https://print-platform-api.onrender.com";
// Leave "" to call the same origin (only if the API is served from this same domain).
window.API_BASE = "";
